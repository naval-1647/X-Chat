# FastAPI and ASGI Server (compatible versions)
fastapi==0.103.2
uvicorn==0.23.2

# Database - MongoDB (compatible versions)
motor==3.3.1
pymongo==4.5.0

# Authentication & Security (no bcrypt compilation issues)
python-jose==3.3.0
passlib==1.7.4
python-multipart==0.0.6

# Environment & Configuration (older stable versions)
python-dotenv==1.0.0
pydantic==1.10.13

# HTTP Client for external APIs
httpx==0.25.0
aiofiles==23.2.1

# Rate limiting
slowapi==0.1.9

# Email validation (simple version)
email-validator==1.3.1

# Production server
gunicorn==21.2.0